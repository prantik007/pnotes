import { Flex, Heading } from "@chakra-ui/react";
import React from "react";

const About = () => {
  return <Flex justify={'center'} align='center' mt={60}>
  <Heading color={'pink.600'}>In Progress...</Heading>
  </Flex>
};

export default About;
